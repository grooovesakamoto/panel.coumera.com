# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Cloud SQLで使用するための接続情報
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=coumera

# Google Cloud
GOOGLE_CLOUD_PROJECT="your_project_id"
GOOGLE_APPLICATION_CREDENTIALS="./service-account-key.json"
GOOGLE_CLOUD_STORAGE_BUCKET="your_storage_bucket"

# BigQuery
BIGQUERY_DATASET_ID="your_dataset_id"

# Authentication (本番環境)
NEXTAUTH_URL="https://your-production-url.example.com"
NEXTAUTH_SECRET="your_random_secret_here"

# Application (本番環境)
NEXT_PUBLIC_APP_URL="https://your-production-url.example.com"

# Local development settings
POSTGRES_USER=your_local_postgres_user
POSTGRES_PASSWORD=your_local_postgres_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=coumera

# Next.js (開発環境)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database (PostgreSQL)
DATABASE_URL="postgresql://username:password@localhost:5432/coumera?schema=public"
SHADOW_DATABASE_DISABLED=true

# Authentication (開発環境)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET="development-secret-key-change-in-production"

# Development Mode
NODE_ENV="development" 